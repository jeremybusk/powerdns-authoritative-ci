name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Run before
        run: ./before-script
      - name: Pre deploy test install
        run: ./install
